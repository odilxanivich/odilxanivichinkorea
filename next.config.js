/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // if you need external images: add domains
  images: {
    domains: ['images.unsplash.com'],
  },
}
module.exports = nextConfig
