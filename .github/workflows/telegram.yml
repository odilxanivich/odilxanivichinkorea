name: Notify Telegram on Markdown Push

on:
  push:
    paths:
      - '**/*.md'

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Find changed Markdown file
        run: |
          FILE=$(git diff --name-only ${{ github.event.before }} ${{ github.sha }} | grep '\.md$' | head -n 1)
          echo "FILE=$FILE" >> $GITHUB_ENV

          if [ -z "$FILE" ]; then
            echo "TITLE=Yangi post" >> $GITHUB_ENV
            echo "DESC=Yangi maqola qo‘shildi" >> $GITHUB_ENV
            exit 0
          fi

          # Parse front-matter safely (only the YAML block at the top)
          FRONT=$(awk 'NR>1 && $0=="---"{exit} NR>1{print}' "$FILE")
          TITLE=$(echo "$FRONT" | sed -n 's/^[[:space:]]*title:[[:space:]]*"\(.*\)"/\1/p')
          EXCERPT=$(echo "$FRONT" | sed -n 's/^[[:space:]]*excerpt:[[:space:]]*"\(.*\)"/\1/p')
          SUMMARY=$(echo "$FRONT" | sed -n 's/^[[:space:]]*summary:[[:space:]]*"\(.*\)"/\1/p')

          [ -z "$TITLE" ] && TITLE="Yangi post"
          if [ -n "$EXCERPT" ]; then
            DESC="$EXCERPT"
          else
            DESC="$SUMMARY"
          fi
          [ -z "$DESC" ] && DESC="Yangi maqola qo‘shildi"

          echo "TITLE=$TITLE" >> $GITHUB_ENV
          echo "DESC=$DESC" >> $GITHUB_ENV

      - name: Debug values
        run: |
          echo "FILE=${{ env.FILE }}"
          echo "TITLE=${{ env.TITLE }}"
          echo "DESC=${{ env.DESC }}"

      - name: Send Telegram message
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          TITLE: ${{ env.TITLE }}
          DESC: ${{ env.DESC }}
        run: |
          curl -s -X POST "https://api.telegram.org/bot$TELEGRAM_TOKEN/sendMessage" \
            -d chat_id="$TELEGRAM_CHAT_ID" \
            -d parse_mode=HTML \
            -d text="🔥 Yangi post qo‘shildi!%0A%0A📝 Sarlavha: $TITLE%0A💡 Tavsif: $DESC"
