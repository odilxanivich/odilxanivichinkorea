name: TEST SECRETS ONLY
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "TOKEN starts with: ${{ secrets.TELEGRAM_BOT_TOKEN != '' && 'OK' || 'MISSING' }}"
          echo "CHATID starts with: ${{ secrets.TELEGRAM_CHAT_ID != '' && 'OK' || 'MISSING' }}"
          curl -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
            -d chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
            -d text="🧪 SECRETS TEST $(date)"
