name: Notify Telegram on Markdown Push

on:
  push:
    paths:
      - '**/*.md'

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Install yq
        run: sudo apt-get update && sudo apt-get install -y yq

      - name: Find changed Markdown file
        run: |
          FILE=$(git diff-tree --no-commit-id --name-only -r ${{ github.sha }} | grep '\.md$' | head -n1)
          echo "FILE=$FILE" >> $GITHUB_ENV
          if [ -z "$FILE" ]; then
            echo "TITLE=Yangi post" >> $GITHUB_ENV
            echo "DESC=Yangi maqola qo‘shildi" >> $GITHUB_ENV
            exit 0
          fi

      - name: Parse front-matter
        run: |
          TITLE=$(yq e '.title' "$FILE")
          EXCERPT=$(yq e '.excerpt' "$FILE")
          SUMMARY=$(yq e '.summary' "$FILE")
          DESC=${EXCERPT:-$SUMMARY}
          TITLE=${TITLE:-"Yangi post"}
          DESC=${DESC:-"Yangi maqola qo‘shildi"}
          echo "TITLE=$TITLE" >> $GITHUB_ENV
          echo "DESC=$DESC" >> $GITHUB_ENV

      - name: Debug
        run: |
          echo "FILE=${{ env.FILE }}"
          echo "TITLE=${{ env.TITLE }}"
          echo "DESC=${{ env.DESC }}"

      - name: Send Telegram message
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          curl -s -X POST "https://api.telegram.org/bot$TELEGRAM_TOKEN/sendMessage" \
            -d chat_id="$TELEGRAM_CHAT_ID" \
            -d parse_mode=HTML \
            -d text="🔥 Yangi post qo‘shildi!%0A%0A📝 Sarlavha: $TITLE%0A💡 Tavsif: $DESC"
